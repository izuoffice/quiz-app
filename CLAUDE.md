# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## プロジェクト概要

- プロジェクト名: quiz-app
- 内容: 一般常識クイズアプリ
- 技術スタック: HTML / CSS / JavaScript(素のフロントエンド実装。フレームワークやビルドツールは使用しない想定)

現時点でリポジトリは初期状態(ファイルなし)です。以下は今後の開発方針の指針であり、実装が進んだ際には実際のディレクトリ構成やコマンドに合わせて随時更新してください。

## 開発コマンド

ビルドツールやパッケージマネージャは導入されていません。`index.html` をブラウザで直接開くか、簡易HTTPサーバーで配信して動作確認してください。

```bash
# 例: 簡易サーバーで起動する場合(Python がある場合)
python -m http.server 8000

# 例: Node.js がある場合(npx 経由)
npx serve .
```

lint・テストの仕組みは未導入です。導入した場合はこのセクションに実行コマンドを追記してください。

## アーキテクチャ方針

- ビルドステップを持たない素のHTML/CSS/JavaScriptで構成する。
- クイズの問題データ、出題・採点ロジック、画面表示(DOM操作)の関心を分離して実装すること(例: データはJSON/JSオブジェクトとして分離し、ロジックとUI描画を別ファイルに分ける、など)。
- コードが増えてきたら、実際のファイル構成・データフローをこのファイルに追記し、複数ファイルにまたがる全体設計(問題データの持ち方、状態管理の方法、画面遷移の仕組みなど)がひと目でわかるようにする。
